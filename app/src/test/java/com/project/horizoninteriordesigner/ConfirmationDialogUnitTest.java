package com.project.horizoninteriordesigner;


import org.junit.runner.RunWith;
import org.mockito.runners.MockitoJUnitRunner;

@RunWith(MockitoJUnitRunner.class)
public class ConfirmationDialogUnitTest {

   /* ConfirmationDialog validDialog;
    ConfirmationDialog invalidDialog;


    @Mock
    Context mockContext;

    @Before
    public void initMock() {
        mockContext = mock(Context.class);
    }

    @Test
    public void confirmationDialog_isCreated() {
        validDialog = new ConfirmationDialog(mockContext);

        validDialog.createDialog(null, null);
    }*/
}
